# Railway Python deployment - let Python provider handle pip
providers = ["python"]

[phases.setup]
aptPkgs = ["postgresql-client", "libpq-dev", "gcc"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"