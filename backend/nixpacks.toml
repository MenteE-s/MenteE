# Railway Python deployment
providers = ["python"]

[phases.setup]
aptPkgs = ["postgresql-client", "libpq-dev", "gcc"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"