# Database
# DATABASE_URL=sqlite:///instance/recruai_dev.db
# For production, use PostgreSQL:
# DATABASE_URL=postgresql://username:password@host:port/database

# JWT Secret Keys
SECRET_KEY=dev-secret-key-change-in-production
JWT_SECRET_KEY=dev-jwt-secret-change-in-production

# Frontend Origin for CORS
FRONTEND_ORIGIN=http://localhost:3000,http://localhost:3001
# Production: lock to your domains (comma-separated)
# FRONTEND_ORIGIN=https://www.yourdomain.com,https://app.yourdomain.com

# Supabase Configuration (for production file storage)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Cloudinary (alternative to Supabase)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# Flask Configuration
FLASK_DEBUG=0
FLASK_ENV=production

# Cookies & Security (enable when serving over HTTPS)
SESSION_COOKIE_SECURE=1
JWT_COOKIE_SECURE=1
ENABLE_HSTS=1