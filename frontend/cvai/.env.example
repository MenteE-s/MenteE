# Frontend environment variables (Netlify set these for production)
# Set REACT_APP_API_BASE to your Railway backend URL, e.g. https://my-cvai-backend.up.railway.app
REACT_APP_API_BASE=http://localhost:5000
# Production example:
# REACT_APP_API_BASE=https://api.yourdomain.com

# Optionally define the frontend origin for backend CORS (when deploying backend to Railway)
REACT_APP_FRONTEND_ORIGIN=http://localhost:3000
# Production example (set in backend .env FRONTEND_ORIGIN):
# REACT_APP_FRONTEND_ORIGIN=https://www.yourdomain.com
